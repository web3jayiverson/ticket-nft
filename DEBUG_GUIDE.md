# 调试指南

## 问题1：活动只显示一个

### 已完成的修复
✅ Home.jsx 现在使用10个独立的 `useReadContract` 调用
✅ 正确检查 `info.isActive` 来过滤活动
✅ 合并 localStorage 中的图片和其他元数据

### 调试步骤

1. **打开浏览器控制台**（按 F12）
2. **切换到 Console 标签页**
3. **连接钱包**
4. **查找以下日志**：

```
========== 开始加载活动 ==========
✅ 添加活动: ID=0, 名称=XXX
✅ 添加活动: ID=1, 名称=XXX
========== 加载完成，共 X 个活动 ==========
```

### 预期行为

- ✅ 应该看到多个"添加活动"的日志（如果合约有多个活动）
- ✅ 每个活动应该有唯一的 ID (0, 1, 2, ...)
- ✅ 最后应该显示总的活动数量

### 如果只看到一个活动

**可能原因：**

1. **合约中只有一个活动**
   - 检查控制台中只显示一次"添加活动"
   - 说明合约中确实只有一个活动被创建
   - 解决：使用管理后台创建更多活动

2. **其他活动未激活**
   - 检查 `isActive` 字段
   - 在管理后台检查活动状态
   - 解决：激活其他活动

3. **读取失败**
   - 检查控制台错误信息
   - 可能是网络问题或合约地址错误
   - 解决：刷新页面或检查网络连接

### 如何在合约中创建多个活动

1. 使用管理员钱包连接
2. 点击"管理后台"
3. 点击"创建活动"
4. 填写信息并创建

## 问题2：管理员页面不显示

### 已完成的修复
✅ App.jsx 添加了详细的调试日志
✅ Navbar.jsx 添加了调试日志
✅ isOwner 状态正确更新

### 调试步骤

1. **打开浏览器控制台**（按 F12）
2. **连接管理员钱包**
3. **查找以下日志**：

```
=== 检查管理员权限 ===
Owner: 0x...
Address: 0x...
IsConnected: true
Owner check result: true/false
=== Navbar 渲染 ===
isOwner: true/false
Final navItems: [...]
```

### 预期行为

如果连接的是管理员钱包：
```
Owner: 0x... (合约owner地址)
Address: 0x... (当前钱包地址)
Owner check result: true  ← 应该是true
```

```
=== Navbar 渲染 ===
isOwner: true  ← 应该是true
Final navItems: [
  { id: 'home', ... },
  { id: 'my-tickets', ... },
  { id: 'verify', ... },  ← 应该看到这两个
  { id: 'admin', ... }   ← 应该看到这两个
]
```

### 如果管理员页面不显示

**可能原因1：不是管理员钱包**

检查日志：
```
Owner check result: false  ← 当前钱包不是owner
```

**解决方法：**
1. 使用部署合约的owner钱包地址
2. 或者在合约中修改owner地址

**可能原因2：合约owner读取失败**

检查日志：
```
Owner: undefined 或 null
```

**解决方法：**
1. 检查合约地址是否正确
2. 检查是否在正确的网络（Sepolia）
3. 检查合约是否部署成功

**可能原因3：地址格式问题**

检查日志中的地址：
```
Owner: 0xABC...
Address: 0xDEF...
```

注意大小写和格式（0x前缀）

**解决方法：**
- 确保 `toLowerCase()` 转换正确
- 检查地址是否完整（40字符）

### 如何获取合约owner地址

**方法1：使用Etherscan**
1. 访问 https://sepolia.etherscan.io
2. 搜索合约地址：`0x98c6af4c16F492ECD21206f17Ace979CcF021f98`
3. 在"Read Contract"标签页查看 `owner()` 函数

**方法2：使用控制台**
在浏览器控制台运行：
```javascript
// 确保已连接钱包
const { useReadContract } = await import('wagmi');
// 或直接查看App.jsx的日志
```

**方法3：使用 Remix**
1. 在Remix中部署合约时记录部署者地址
2. 部署者地址就是owner

## 常见问题

### Q: 为什么控制台没有日志？

A: 检查是否在正确的标签页（Console），并且过滤级别设置为"All"。

### Q: 日志显示很多错误？

A: 提供完整的错误信息，包括堆栈跟踪。

### Q: 如何确认钱包地址？

A:
1. 在MetaMask中点击钱包图标
2. 查看"Account 1"等账户
3. 复制完整的地址（0x开头的40位字符）

### Q: 切换钱包后isOwner没有更新？

A: 刷新页面，确保useEffect重新执行。

## 验证修复

### 活动显示验证
- [ ] 控制台显示多个"添加活动"日志
- [ ] 页面显示多个活动卡片
- [ ] 每个活动有正确的名称、价格、场地

### 管理员页面验证
- [ ] 控制台显示"Owner check result: true"
- [ ] 导航栏显示"门票核销"和"管理后台"
- [ ] 点击能访问这两个页面

## 临时解决方案

如果问题持续，可以：

1. **临时跳过权限检查**
   在App.jsx中直接设置 `setIsOwner(true)` 进行测试

2. **使用本地数据**
   在Home.jsx中硬编码一些活动数据用于测试

3. **检查合约状态**
   使用Etherscan查看合约的实际状态

## 需要帮助？

如果调试后问题仍未解决，请提供：

1. **完整的控制台日志**
2. **连接的钱包地址**
3. **合约地址**（确认是否正确）
4. **网络**（是否在Sepolia）

---

**最后更新**: 已添加详细调试日志，请按照步骤操作并查看控制台输出。
